<%= form_for(@zone_entry) do |f| %>
  <% if @zone_entry.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@zone_entry.errors.count, "error") %> prohibited this zone_entry from being saved:</h2>

      <ul>
      <% @zone_entry.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :zone %><br />
    <%= f.number_field :zone %>
  </div>
  <div class="field">
    <%= f.label :duration %><br />
    <%= f.text_field :duration %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
